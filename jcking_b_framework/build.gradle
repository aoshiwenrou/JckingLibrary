apply from: rootProject.getRootDir().getAbsolutePath() + "/libconfig.gradle"

dependencies {
    implementation "com.android.support:multidex:${rootProject.multidexVersion}"
    implementation project(':jcking_b_log')
}
